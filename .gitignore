# Secrets / local credentials — never commit
.env
.env.*
*.pem
*.key
credentials
credentials.csv
.aws/
.docker/

# Runtime state from a live POC run (account-specific IDs)
.state.env

# Terraform (sketch under infra/ — never commit state or local plugins)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Local editor / OS
.DS_Store
*.swp
__pycache__/
*.pyc

# Docker build junk
.dockerignore.bak
